services:
  - type: web
    name: photonfreak-api
    env: python
    region: oregon  # or frankfurt, singapore, etc.
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true